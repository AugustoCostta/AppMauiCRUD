<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:AppMauiBTG.ViewModels"
             x:Class="AppMauiBTG.Views.MainPage"
             Title="Gerenciamento de Clientes">

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*">
        
        <Grid Grid.Row="0" Padding="10" HorizontalOptions="End">
            <Button Text="Adicionar Cliente" Command="{Binding AddCustomerCommand}" Margin="5"/>
        </Grid>
        <StackLayout Padding="15,10,15,10" Grid.Row="1">
            
            <CollectionView ItemsSource="{Binding Customers}">
                
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="05" />
                </CollectionView.ItemsLayout>
                
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="Black" CornerRadius="7" Padding="10" HasShadow="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding Name, StringFormat='Name: {0}'}" FontAttributes="Bold" Grid.Row="0" Grid.Column="0" TextColor="Black"/>
                                <Label Text="{Binding Lastname, StringFormat='Lastname: {0}'}" Grid.Row="1" Grid.Column="0" TextColor="Black"/>
                                <Label Text="{Binding Age, StringFormat='Age: {0}'}" Grid.Row="2" Grid.Column="0" TextColor="Black"/>
                                <Label Text="{Binding Address, StringFormat='Address: {0}'}" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" TextColor="Black"/>

                                <StackLayout Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="10" Margin="0,5,0,0">
                                    <Button Text="Editar" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=EditCustomerCommand}" CommandParameter="{Binding .}" />
                                    <Button Text="Excluir" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=DeleteCustomerCommand}" CommandParameter="{Binding .}" />
                                </StackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
            
        </StackLayout>

    </Grid>
</ContentPage>
